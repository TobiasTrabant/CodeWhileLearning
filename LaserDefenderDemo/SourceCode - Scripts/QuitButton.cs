using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuitButton : MonoBehaviour {

    float topLeftY;
    float topLeftX;

    // Use this for initialization
    void Start () {
    //    float distance = transform.position.z - Camera.main.transform.position.z;
    //    Vector3 topLeftMin = Camera.main.ViewportToWorldPoint(new Vector3(0, 1, distance));
    //    topLeftX = topLeftMin.x;
    //    topLeftY = topLeftMin.y;
    //    this.transform.position = new Vector3(topLeftX, topLeftY, 0);
    //    Debug.Log(topLeftX);
    //    Debug.Log(topLeftY);

    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
